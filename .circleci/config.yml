version: 2
jobs:
  build:
    working_directory: ~/code
    macos:
      xcode: 11.3.0
    #docker:
    #  - image: circleci/node:10
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: npm install --save

      - run:
          name : Run all test
          command : |
            npm run test