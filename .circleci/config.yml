version: 2
jobs:
  build:
    working_directory: ~/code
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: npm install --save

      - run:
          name : Run all test
          command : |
            npm run test